using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace DBConnections
{
    public class DBConstants
    {


        public const String noInfoError = "No values for connection have been set";
        public const String noServerError = "Server must have a value";
        public const String noUserError = "User must have a value";
        public const String noPasswordError = "Password must have a value";
        public const String noDBerror = "Database must have a value";
        public const String errorGeneralConnect = "Connections should only succeed with all values";


    }
}
